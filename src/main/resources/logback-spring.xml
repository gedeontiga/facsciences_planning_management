<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for error logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- Appender for Spring Boot banner and startup messages -->
    <appender name="STARTUP" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Suppress non-error logs for application classes -->
    <logger name="com.facsciences_planning_management" level="ERROR" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Allow Spring Boot banner and startup messages, including port and time -->
    <logger name="org.springframework.boot" level="INFO" additivity="false">
        <appender-ref ref="STARTUP" />
    </logger>
    <logger name="org.springframework.web" level="INFO" additivity="false">
        <appender-ref ref="STARTUP" />
    </logger>
    <logger name="org.apache.catalina" level="INFO" additivity="false">
        <appender-ref ref="STARTUP" />
    </logger>
    <logger name="org.apache.tomcat" level="INFO" additivity="false">
        <appender-ref ref="STARTUP" />
    </logger>
</configuration>